# This file was generated by liblab | https://liblab.com/

from __future__ import annotations
from typing import List
from .utils.json_map import JsonMap
from .utils.base_model import BaseModel
from .utils.sentinel import SENTINEL
from .sort_order import SortOrder


@JsonMap({})
class PaginationInfo(BaseModel):
    """PaginationInfo

    :param page_offset: The page number that was fetched, defaults to None
    :type page_offset: int, optional
    :param page_size: The size of the page fetched, defaults to None
    :type page_size: int, optional
    :param sort_order: The sort order passed in originally, defaults to None
    :type sort_order: List[SortOrder], optional
    :param total_pages: The total number of pages that can be fetched, defaults to None
    :type total_pages: int, optional
    :param total_records: The total number of records fetchable, defaults to None
    :type total_records: int, optional
    """

    def __init__(
        self,
        page_offset: int = SENTINEL,
        page_size: int = SENTINEL,
        sort_order: List[SortOrder] = SENTINEL,
        total_pages: int = SENTINEL,
        total_records: int = SENTINEL,
        **kwargs,
    ):
        """PaginationInfo

        :param page_offset: The page number that was fetched, defaults to None
        :type page_offset: int, optional
        :param page_size: The size of the page fetched, defaults to None
        :type page_size: int, optional
        :param sort_order: The sort order passed in originally, defaults to None
        :type sort_order: List[SortOrder], optional
        :param total_pages: The total number of pages that can be fetched, defaults to None
        :type total_pages: int, optional
        :param total_records: The total number of records fetchable, defaults to None
        :type total_records: int, optional
        """
        if page_offset is not SENTINEL:
            self.page_offset = page_offset
        if page_size is not SENTINEL:
            self.page_size = page_size
        if sort_order is not SENTINEL:
            self.sort_order = self._define_list(sort_order, SortOrder)
        if total_pages is not SENTINEL:
            self.total_pages = total_pages
        if total_records is not SENTINEL:
            self.total_records = total_records
        self._kwargs = kwargs
